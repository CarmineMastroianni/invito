{
  "name": "wedding-invitation",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Interactive scratch-off wedding invitation for Cristina & Carmine",
  "author": "Carmine",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "docker:build": "docker build -t wedding-invitation .",
    "docker:run": "docker run -p 8080:8080 wedding-invitation",
    "docker:stop": "docker stop $(docker ps -q --filter ancestor=wedding-invitation)",
    "gcloud:build": "gcloud builds submit --tag gcr.io/PROJECT_ID/wedding-invitation",
    "gcloud:deploy": "gcloud run deploy wedding-invitation --image gcr.io/PROJECT_ID/wedding-invitation --platform managed --region europe-west1 --allow-unauthenticated --port 8080 --memory 256Mi --cpu 1"
  },
  "devDependencies": {
    "terser": "^5.46.0",
    "vite": "^7.3.1"
  }
}
